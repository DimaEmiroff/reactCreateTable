import React from 'react';

const ButtonOnLoad = () => {
    return (
        <div>
            <button className={'btn_onload'}>On Load</button>
        </div>
    );
};

export default ButtonOnLoad;